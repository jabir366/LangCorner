#!/bin/env python3
#what we are having?
#
# we have a list of containers running, a list of other parameters just make all of them a configurable item
#
import json
import sys

#functions needed 
# get_subsc_data(cont_id)
# get_cont_data(cont_id)
# get_diff(old_data, new_data)

#process
# we have n containers and n calls are made and n subscribers are there

def main():
    print("hello")
if __name__ == '__main__':
    main()
