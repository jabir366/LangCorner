
#  whenever the music stops one person will be out

#push function will push the new data at the beginning
def main():
    num_players = int(input("Enter the number of players"))
    ll = LinkedList()

    players=[ll.push(0) for i in range(num_players)]
        
    #value 0 means no ball
    #value 1 means the ball is the player
    handler = register("music_event")
    for i,player in enumerate(players):
        start=
        while(True):
            register()
            time.sleep()
            temp=start 
            start=start.next
            




        
    
def onrecieve(player_index,handler):
    remove(playe_index)
    # a , b, c
    temp.next=start.next

    



    

